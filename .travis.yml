language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
# Command to install dependencies
install:
  - pip install -r weather_app/requirements.txt
# command to run tests
script:
  - pytest weather_app/test/test.py # or py.test for Python versions 3.5 and below